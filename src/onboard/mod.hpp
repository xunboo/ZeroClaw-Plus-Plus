#pragma once

#include "wizard.hpp"

namespace zeroclaw::onboard {
    using wizard::run_wizard;
    using wizard::run_channels_repair_wizard;
    using wizard::run_quick_setup;
    using wizard::run_models_refresh;
}
